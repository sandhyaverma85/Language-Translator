# Flask Application Settings
FLASK_APP=app.py
FLASK_ENV=development
PORT=5000

# Optional: Translation Service configuration
# By default, the application uses deep-translator (Google Translate engine) which requires no API key!
# If you want to use a custom API or proxy in the future, configure it here.
TRANSLATION_PROVIDER=google
